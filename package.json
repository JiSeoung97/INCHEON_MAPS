{
  "scripts": {
    "dev": "cross-env NODE_ENV=development node proxy.js",
    "start": "cross-env NODE_ENV=production node proxy.js",
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cross-env": "^10.0.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^6.0.2",
    "@rollup/plugin-terser": "^0.4.4",
    "clean-css": "^5.3.3",
    "fs-extra": "^11.3.2",
    "rollup": "^4.52.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
